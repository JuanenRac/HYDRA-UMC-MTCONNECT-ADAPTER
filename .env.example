# =============================================================================
# HYDRA-UMC-MTCONNECT-ADAPTER - Environment variable template
# Copyright (C) 2026 JuanenRac (Electro Hobby 3D) <electrohobby3d@gmail.com>
# GPL-3.0 - see LICENSE
# =============================================================================
# This service does not auto-load .env: export these values through systemd,
# Docker or the invoking shell. HTTP port exposed to agents and collectors. Default: 5000.
PORT=5000

# Real minimum interval (ms) between actual reads of the machine source
# backing GET /current's Samples block (see src/reader.ts's CachedReader) -
# a request within this window is served the cached reading instead of
# polling the source again. Default: 1000.
POLL_INTERVAL_MS=1000
